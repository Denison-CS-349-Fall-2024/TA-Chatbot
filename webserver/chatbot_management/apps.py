from django.apps import AppConfig

class ChatbotManagementConfig(AppConfig):
    """
    Configuration for the Chatbot Management app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'chatbot_management'